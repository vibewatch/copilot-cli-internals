# Latest automated Copilot CLI package update

> Generated by `scripts/update-copilot-cli-docs.mjs`. Treat atlas and changelog deltas as leads; confirm behavior in the extracted runtime or adjacent SDK/schema files before publishing authoritative prose.

Generated: `2026-08-31T06:52:42.714Z`

## Package identity

| Field | Previous | Current |
|---|---:|---:|
| Package version | `1.0.71` | `1.0.82` |
| Build commit | `3286dc4` | `123e53c` |
| `app.js` bytes | `9,089,681` | `7,483,179` |
| `app.js` lines | `6,017` | `5,358` |
| Functions | `7,473` | `6,447` |
| Classes | `612` | `499` |
| Declaration blocks | `22,656` | `19,885` |

## Named-surface delta

| Surface | Added | Removed |
|---|---:|---:|
| `envVars` | +44 | -62 |
| `featureKeys` | +291 | -86 |
| `experimentFlagStrings` | +3 | -39 |
| `eventStrings` | +18 | -187 |
| `jsonRpcMethods` | +12 | -200 |
| `confirmedSlashCommands` | +0 | -0 |
| `slashCommandCandidates` | +5 | -4 |
| `toolNameHits` | +0 | -3 |
| `packagedDefinitions` | +0 | -0 |

### envVars

Added:

- `COPILOT_AHP_CLIENT_ID`
- `COPILOT_AHP_DISCOVER`
- `COPILOT_AHP_HOST_BIN`
- `COPILOT_AHP_HOST_SETTLE_MS`
- `COPILOT_AHP_SHARE_BIND`
- `COPILOT_AHP_URL`
- `COPILOT_ASSISTED_APPROVAL`
- `COPILOT_CLI_PATH`
- `COPILOT_DEBUG_ENTRA_BROKER_URL`
- `COPILOT_E2E_TEST_HOOKS`
- `COPILOT_EVENTS_LOG_INCLUDE_SUBAGENTS`
- `COPILOT_HANDOFF_CONFIRMATION`
- `COPILOT_MASCOT_BLINK`
- `COPILOT_MEDIAREMOTE_ADAPTER_DIR`
- `COPILOT_PKG_CACHE_HOME`
- `COPILOT_PLAN_THEN_AUTOPILOT`
- `COPILOT_PROMPT_TURN_DELIMITER`
- `COPILOT_PROVIDER_GHES_HOST`
- `COPILOT_PROVIDER_GHES_TOKEN`
- `COPILOT_PROVIDER_HEADERS`
- `COPILOT_RESTART_BOOTSTRAP`
- `COPILOT_TEST_DISABLE_INTERRUPTED_SESSION_RESTORE`
- `COPILOT_TEST_DROP_CONTEXT_CHANGED_EVENTS`
- `COPILOT_TEST_EXTENSION_RELOAD_COMPLETE_FILE`
- `COPILOT_TEST_FAIL_SPLIT_SWAP_AT`
- `COPILOT_TEST_FAIL_SPLIT_SWAP_STAGE`
- `COPILOT_TEST_IDE_AUTO_CONNECT_RETRY_BASE_MS`
- `COPILOT_TEST_IDE_TEARDOWN_WAIT_MS`
- `COPILOT_TEST_INITIAL_EXTENSION_CONFIGURE_RETURN_RELEASE_FILE`
- `COPILOT_TEST_MCP_SLOW_CONNECTION_THRESHOLD_MS`
- `COPILOT_TEST_PRE_PARK_GATE_FILE`
- `COPILOT_TEST_PRE_SEND_DELAY_MS`
- `COPILOT_TEST_PRE_SEND_STALE_IDLE`
- `COPILOT_TEST_REASONING_CLEANUP_DELAY_MS`
- `COPILOT_TEST_RESTART_CAPABILITY`
- `COPILOT_TEST_RESTART_CAPABILITY_GATE`
- `COPILOT_TEST_RESTART_FAIL_INDICES`
- `COPILOT_TEST_RESTART_HANDOFF_FAILURE`
- `COPILOT_TEST_RESTART_TARGET_VERSION`
- `COPILOT_TEST_RESTART_UNEXPECTED_RESTORE_FAILURE`
- ...and 4 more.

Removed:

- `COPILOT_AGENTS_TAB`
- `COPILOT_AGENT_DEBUG`
- `COPILOT_ALLOW_GET_PROVIDER_ENDPOINT`
- `COPILOT_ASSOCIATE_COMPUTE`
- `COPILOT_CI`
- `COPILOT_CLI`
- `COPILOT_CLI_DIST_DIR`
- `COPILOT_COMPUTER_USE_`
- `COPILOT_COMPUTER_USE_LINUX`
- `COPILOT_DEBUG_CONTENT_EXCLUSION_API_URL`
- `COPILOT_DEBUG_DEVICE_CODE_POLL_INTERVAL_MS`
- `COPILOT_DEBUG_RATELIMIT_SESSION_REM`
- `COPILOT_DEBUG_RATELIMIT_WEEKLY_REM`
- `COPILOT_DEBUG_SEND_INBOX_DELAY_MS`
- `COPILOT_DEBUG_SKIP_LAUNCH_CHECKS`
- `COPILOT_DEBUG_VERSION_DEPRECATED_WARNING`
- `COPILOT_DISABLE_RUST_RIPGREP`
- `COPILOT_EMBEDDING_ONLY_SKILLS`
- `COPILOT_ENABLE_SECRET_FILTERING`
- `COPILOT_ENTERPRISE_ALLOWLIST_REPO`
- `COPILOT_ENTERPRISE_ALLOWLIST_URL`
- `COPILOT_EXP_CLIENT_ID`
- `COPILOT_EXP_EXTENSION_NAME`
- `COPILOT_EXP_TIMEOUT_MS`
- `COPILOT_FEATURE_FLAGS`
- `COPILOT_FORCE_EMBEDDING_RETRIEVAL`
- `COPILOT_GH`
- `COPILOT_HMAC_KEY`
- `COPILOT_KEEP_TEMP_FILES`
- `COPILOT_LARGE_OUTPUT_THRESHOLD_BYTES`
- `COPILOT_MCP_COPILOT_SPACES_ENABLED`
- `COPILOT_MCP_EMIT_PENDING_SETUP_LOGS`
- `COPILOT_MCP_STARTUP_CONCURRENCY`
- `COPILOT_MC_ACCESS_TOKEN`
- `COPILOT_MC_REPO_IDS`
- `COPILOT_ODR_DEBUG`
- `COPILOT_PERMISSION_MODE`
- `COPILOT_PLUGIN_DIR_ONLY`
- `COPILOT_PLUGIN_ROOT`
- `COPILOT_PROVIDER_MODEL_LIMITS_ID`
- ...and 22 more.

### experimentFlagStrings

Added:

- `copilot_cli_default_model_override`
- `copilot_cli_model_selection_reset_cadence_seconds`
- `copilot_cli_worktree_default_branch`

Removed:

- `copilot_cli_app_install_nudge`
- `copilot_cli_async_only_shell`
- `copilot_cli_background_task_notification_payloads`
- `copilot_cli_bash_output_compaction`
- `copilot_cli_cloud_session_search_sidekick_agent`
- `copilot_cli_compact_task_tool_prompt`
- `copilot_cli_dynamic_instructions_retrieval_arm`
- `copilot_cli_dynamic_instructions_retrieval_mcp`
- `copilot_cli_fides_ifc`
- `copilot_cli_gemini_validated_tool_choice`
- `copilot_cli_github_context_sidekick_agent`
- `copilot_cli_gpt_5_4_mini_for_explore`
- `copilot_cli_gpt_default_model`
- `copilot_cli_lsp_services_prompt`
- `copilot_cli_mcp_apps`
- `copilot_cli_mcp_enterprise_allowlist`
- `copilot_cli_multi_turn_agents`
- `copilot_cli_multi_turn_routing`
- `copilot_cli_native_web_search`
- `copilot_cli_no_outer_loop_truncation`
- `copilot_cli_no_planning`
- `copilot_cli_no_view_line_numbers`
- `copilot_cli_preserve_reasoning`
- `copilot_cli_read_agent_incremental_reads`
- `copilot_cli_reasoning_only_continuation`
- `copilot_cli_reasoning_summaries_off_by_default`
- `copilot_cli_remote_export_max_events_per_flush`
- `copilot_cli_remote_export_safety_interval_ms`
- `copilot_cli_search_subagent`
- `copilot_cli_search_tool_output_compaction`
- `copilot_cli_session_search_sidekick_agent`
- `copilot_cli_skills_list_in_system_prompt`
- `copilot_cli_subagent_parallelism_prompts`
- `copilot_cli_subconscious`
- `copilot_cli_tgrep`
- `copilot_cli_tool_search_anthropic`
- `copilot_cli_tool_search_builtin_anthropic`
- `copilot_cli_tool_search_openai`
- `copilot_cli_websocket_responses_persistent`

### eventStrings

Added:

- `agent.interrupted`
- `assistant.fusion_phase_completed`
- `assistant.fusion_phase_failed`
- `assistant.fusion_phase_started`
- `assistant.turn_retry`
- `session.agent.setPrompt`
- `session.contentExclusion.checkPaths`
- `session.context_cleared`
- `session.db`
- `session.fusion_commit_started`
- `session.fusion_completed`
- `session.fusion_handoff`
- `session.fusion_resolved`
- `session.fusion_route_failed`
- `session.fusion_route_started`
- `session.managed_settings_enforced`
- `session.sandbox.getEnforcementStatus`
- `subagent.configured`

Removed:

- `session.agent.deselect`
- `session.agent.getCurrent`
- `session.agent.reload`
- `session.agent.select`
- `session.auth.getStatus`
- `session.auth.setCredentials`
- `session.background`
- `session.canvas.action.invoke`
- `session.canvas.close`
- `session.canvas.list`
- `session.canvas.listOpen`
- `session.canvas.open`
- `session.commands.enqueue`
- `session.commands.execute`
- `session.commands.handlePendingCommand`
- `session.commands.invoke`
- `session.commands.list`
- `session.commands.respondToQueuedCommand`
- `session.completions.getTriggerCharacters`
- `session.completions.request`
- `session.debug.collectLogs`
- `session.eventLog.read`
- `session.eventLog.registerInterest`
- `session.eventLog.releaseInterest`
- `session.eventLog.tail`
- `session.extensions.disable`
- `session.extensions.enable`
- `session.extensions.list`
- `session.extensions.reload`
- `session.extensions.sendAttachmentsToMessage`
- `session.fleet.start`
- `session.foreground`
- `session.getWorkspace`
- `session.gitHubAuth.getStatus`
- `session.gitHubAuth.setCredentials`
- `session.history.abortManualCompaction`
- `session.history.cancelBackgroundCompaction`
- `session.history.compact`
- `session.history.summarizeForHandoff`
- `session.history.truncate`
- ...and 147 more.

### jsonRpcMethods

Added:

- `session.agent.setPrompt`
- `session.contentExclusion.checkPaths`
- `session.context_cleared`
- `session.db`
- `session.fusion_commit_started`
- `session.fusion_completed`
- `session.fusion_handoff`
- `session.fusion_resolved`
- `session.fusion_route_failed`
- `session.fusion_route_started`
- `session.managed_settings_enforced`
- `session.sandbox.getEnforcementStatus`

Removed:

- `session.agent.deselect`
- `session.agent.getCurrent`
- `session.agent.reload`
- `session.agent.select`
- `session.auth.getStatus`
- `session.auth.setCredentials`
- `session.background`
- `session.canvas.action.invoke`
- `session.canvas.close`
- `session.canvas.list`
- `session.canvas.listOpen`
- `session.canvas.open`
- `session.commands.enqueue`
- `session.commands.execute`
- `session.commands.handlePendingCommand`
- `session.commands.invoke`
- `session.commands.list`
- `session.commands.respondToQueuedCommand`
- `session.completions.getTriggerCharacters`
- `session.completions.request`
- `session.debug.collectLogs`
- `session.eventLog.read`
- `session.eventLog.registerInterest`
- `session.eventLog.releaseInterest`
- `session.eventLog.tail`
- `session.extensions.disable`
- `session.extensions.enable`
- `session.extensions.list`
- `session.extensions.reload`
- `session.extensions.sendAttachmentsToMessage`
- `session.fleet.start`
- `session.foreground`
- `session.getWorkspace`
- `session.gitHubAuth.getStatus`
- `session.gitHubAuth.setCredentials`
- `session.history.abortManualCompaction`
- `session.history.cancelBackgroundCompaction`
- `session.history.compact`
- `session.history.summarizeForHandoff`
- `session.history.truncate`
- ...and 160 more.

### confirmedSlashCommands

Added:

- None.

Removed:

- None.

### toolNameHits

Added:

- None.

Removed:

- `context_board`
- `grep_search`
- `read_file`

### packagedDefinitions

Added:

- None.

Removed:

- None.

## Stable release notes

### 1.0.82

Added: 0; improved: 0; fixed: 3.

- **fixed:** A message typed while /worktree or /move is preparing the worktree no longer breaks the switch into it

### 1.0.81

Added: 12; improved: 19; fixed: 21.

- **added:** Ship MCP 2026-07-28 support to CLI, SDK, IDE, and in-memory clients
- **added:** Hooks can now receive the current OpenTelemetry trace context and emit correlated spans: inputs gain `traceparent` (plus `tracestate` when the span has vendor state); command hooks also get env vars.
- **added:** Windows: remote MCP servers protected by Microsoft Entra ID can now sign in through the OS authentication broker (WAM), usually with no prompt at all. Other platforms, `--device-code`, and machines without the broker library keep the existing browser flow.
- **added:** Add xhigh reasoning effort support for Grok 4.6
- **added:** Startup now offers to restore sessions that were still open when their CLI went away, so a crash or a machine restart no longer means reopening each terminal by hand
- **added:** models.list now includes service-published infoMessages and warningMessages per model
- **added:** Add `copilot app` to open the GitHub Copilot app in the current directory
- **added:** Add defaultMode and defaultPermissionMode settings to choose startup mode and approval behavior for new interactive sessions
- **added:** Add --with-token to copilot login to read an auth token from stdin
- **added:** Add support for Gemini 3.7 Flash
- **added:** Add Ctrl+E in /sandbox to open settings.json in your editor
- **added:** Add per-agent usage metrics to --usage-output-file JSON output
- **improved:** Compact the autopilot goal panel to its identity row on a short terminal (a paused goal keeps its resume note), with ctrl+x → g to expand or collapse it by hand
- **improved:** Render the autopilot goal panel as a pinned prompt frame, drop its progress bar for the exact todo count, fold the subagent hint into the row it toggles, and keep its metrics on a narrow pane
- **improved:** Resume large sessions faster by showing recent history first while older messages load.
- **improved:** `x` is now the delete key everywhere: /sandbox config, /settings, /mcp, the sessions dialog and the diff comments summary move off `d`
- **improved:** Auto mode now adapts model selection as your task evolves during a conversation
- **improved:** /plugin now flags installed plugins and marketplaces that have a newer version upstream, and offers an Update action to pull it
- **improved:** Show your last prompt as the inferred objective in the Autopilot status panel
- **improved:** When --no-sandbox is ignored because enterprise policy could not be determined, the notice now says so, and no longer points at an administrator, instead of claiming a policy requires the sandbox. The unsupported-host warning says the same rather than contradicting it.
- **improved:** Show model data retention warnings with links in the /model picker
- **improved:** Path-sourced plugins in a local (directory-source) marketplace now load live from their real directory, so editing one takes effect on `/restart` or a new session — no `/plugin update`
- **improved:** Skills and custom agents are discovered from directories added with --add-dir
- **improved:** Use Ctrl+Space to toggle voice dictation.
- **improved:** A session sandboxed by an enterprise managed policy now says so on the timeline, including when the policy arrives mid-session, instead of leaving the footer's sandbox chip as the only hint that commands are being restricted
- **improved:** forceRemoteSettingsRefresh now fails closed: when set, the cached managed-settings policy is never served or used as a fetch-failure fallback (skipping both the 1h fast path and the 24h stale fallback), so a failed startup fetch blocks on the unconfirmed policy instead of reverting to a possibly-stale cached one. Concretely, until a fresh policy is fetched the session applies the restrictive undetermined-policy posture: non-default MCP servers are blocked, bypass-permissions mode cannot be enabled, and policy-gated plugin install/update mutations are blocked
- **improved:** ACP clients receive subagent IDs, raw event subscriptions, and live title, mode, command, and plan updates
- **improved:** Show each user instruction file separately in /instructions
- **improved:** Managed settings now win per entry for enabledPlugins and extraKnownMarketplaces, so a plugin or marketplace your organization pins can't be overridden locally
- **improved:** Use x to remove scheduled /every and /after prompts in Schedule Manager
- **improved:** Update model configurations
- **fixed:** Repeated read_agent calls now consistently return the full turn history unless since_turn is provided
- **fixed:** Hook lifecycle events (`hook.start`/`hook.end`) from hooks inside a subagent are now recorded on that subagent's session and re-emitted on its parent, instead of being dropped on an internal session.
- **fixed:** Repeatedly resuming the same session no longer crashes while telemetry is being replaced
- **fixed:** An MCP server blocked by an enterprise policy now shows as blocked in /mcp instead of spinning as pending forever
- **fixed:** Fixed an indefinite "Loading…/Resuming…" hang at startup when a repository plugin activates a contributed extension (or another extension reload races the initial load), which previously left the environment stuck on "still waiting on extensions"
- **fixed:** The startup status finishes after extension configuration during plugin reconciliation
- **fixed:** Signing out of an account now clears its cached enterprise managed settings, so signing back in generally re-fetches the policy rather than re-applying the one cached before sign-out
- **fixed:** An enterprise managed-settings policy is no longer rejected when `permissions.disableBypassPermissionsMode` carries an unrecognized value; it is now logged and enforced as `disable`.
- **fixed:** Sandboxed builds on Windows create their scratch caches on first run, so cargo, go, Gradle, and ccache work without a warm cache

### 1.0.80

Added: 0; improved: 1; fixed: 0.

- **improved:** Update model configurations

### 1.0.79

Added: 9; improved: 15; fixed: 19.

- **added:** Add support for enterprise allow-auto-only policy so /allow-all auto works while full allow-all remains blocked.
- **added:** Allow enterprise-managed sandbox policy to enforce a proxy URL while credentials remain user-controlled
- **added:** Agent Plugins spec plugins can now ship extensions under a com.github.copilot/extensions/ directory
- **added:** Add support for the kimi-k3 model
- **added:** Combine `--plan` with `--mode autopilot` to plan first and then implement without waiting for approval
- **added:** Manage multiple concurrent sessions from the Sessions tab and sidebar
- **added:** Add /sandbox policy to show effective sandbox paths, denials, and network access
- **added:** Queue prompts, shell commands, and supported slash commands in local sessions to run in order after the current task finishes
- **added:** Set "autoUpdate": true on an extraKnownMarketplaces entry in your user settings to auto-update its plugins at session start
- **improved:** The /sandbox configuration dialog shows where sandbox settings are stored in settings.json
- **improved:** The /sandbox configuration dialog groups the git, gh, and (on macOS) keychain settings under a new Auth tab, and the settings keys moved from `sandbox.gitAuth`/`sandbox.ghAuth` to `sandbox.auth.git`/`sandbox.auth.gh`. There is no migration: the old keys are ignored in settings files, and SDK requests that still send them are rejected as invalid rather than ignored
- **improved:** Added a `worktreeBaseRef` setting that controls whether `/worktree`, `/worktree new`, and `--worktree` start from HEAD or the remote default branch. All three now default to HEAD; previously `--worktree` started from the remote default branch.
- **improved:** Model picker groups models into Recent, Recommended, New, and other sections, and Shift+Tab switches grouping views.
- **improved:** Large monorepos now use tgrep ([trigram-indexed grep for fast regex search in large codebases](https://github.com/microsoft/tgrep)) instead of ripgrep
- **improved:** Ask user multi-select prompts include an Other option for free-text answers
- **improved:** Improve teleported subagent /tasks navigation with nested tree browsing, current/all and finished-task filters, and a live timeline you can steer
- **improved:** Prompt pinning is off by default; set pinnedPrompts to true to enable it.
- **improved:** Use `/worktree new` to start a new session in a new worktree
- **improved:** Pin the current prompt one row higher, in the row the tab bar already reserves, so it keeps the shape of the prompt it copies while costing the timeline one row less
- **improved:** BREAKING: the sandbox setting `allowDevToolCaches` is renamed `allowDevToolAccess`, since it grants dev-tool config and registries too, not just caches. The old key is no longer read and is ignored silently, so an existing `false` opt-out reverts to the default (on). Rename it in settings.json and in any managed/MDM policy.
- **improved:** /sandbox tags inactive settings as (disabled) and explains why they are locked, and documents dev tool caches in copilot help sandbox
- **improved:** Show "pending · ctrl+c to cancel" for in-flight steering prompts
- **improved:** Make /model session-scoped by default, and use /config model to set defaults for future sessions.
- **improved:** Pin the current prompt as a single line instead of a three-row framed block, so it reads as chrome and returns rows to the timeline; with the tab bar on it sits directly below the tabs and costs the timeline nothing
- **fixed:** A tool directory inside your workspace that is on PATH (.venv/bin, node_modules/.bin, an in-repo GOPATH) no longer turns that part of the workspace read-only in the sandbox
- **fixed:** The `/app` command now opens the current session in the GitHub Copilot desktop app instead of landing on Home with the wrong folder (requires GitHub Copilot app 1.1.3 or later)
- **fixed:** On macOS, a sandbox read-only path nested inside a writable one now stays read-only instead of inheriting the write permission from the wider path
- **fixed:** On macOS, sandboxed commands can use UNIX-domain sockets again, so tools that talk over a local IPC pipe (tsx, vite, esbuild, jest workers) no longer fail with `listen EPERM`
- **fixed:** Sandboxed commands work when the working directory lives on a Windows Dev Drive
- **fixed:** `/theme` now only shows its deprecation notice for a valid color mode, so a mistyped mode no longer suggests an invalid command or hides the notice from your next valid `/theme`.
- **fixed:** Sandboxed git now authenticates to Azure DevOps, GitHub Enterprise Server, GitLab, and other non-GitHub remotes you have stored HTTPS credentials for
- **fixed:** A failed session-history load no longer leaves the timeline permanently empty: the failure was silently discarded, so the transcript stayed blank for the rest of the session with nothing logged. It is now retried, and reported in the transcript and the log if it still fails
- **fixed:** Resuming a long session no longer collapses the timeline's scroll range while history renders in the background: entries that had not finished rendering were published as if they did not exist, so the scrollbar and scroll position jumped until the background render caught up
- **fixed:** Sandboxed wrapper builds (make and friends) get the dev tool caches their recipes need, based on the build manifests in the working directory
- **fixed:** Sandboxed commands can reach the network again on recent Windows builds, where every outbound connection was blocked even with outbound access enabled and no proxy configured
- **fixed:** Plugin custom agents honor deferred-tool-loading frontmatter
- **fixed:** A sandbox that cannot start an MCP server now fails in seconds instead of stalling the session, and sandbox startup failures for both MCP and language servers now say the sandbox was at fault and how to fix or opt out of it
- **fixed:** Compute /context attribution against the Auto-resolved model so token totals are accurate for Free/Student users
- **fixed:** Disabling an extension no longer breaks elicitation, canvases, or tool permission prompts for other extensions
- **fixed:** A prompt stashed with ctrl+s now stays with the session it was typed for, so switching away and back and pressing ctrl+s restores it instead of finding it gone

### 1.0.78

Added: 5; improved: 12; fixed: 9.

- **added:** Add the experimental /new-worktree command to create a new worktree and start a new conversation in it
- **added:** Expose token usage in ACP prompt results and live usage_update notifications
- **added:** Added a forceRemoteSettingsRefresh managed setting that requires a fresh managed-settings fetch on startup
- **added:** Add /permissions to switch between approval modes.
- **added:** ACP mode supports closing sessions with the closeSession request.
- **improved:** Timeline headers show how long each tool call took, right-aligned and ticking live while it runs (for calls of at least 5 seconds). On by default — disable with `/settings showToolDurations`.
- **improved:** First-party plugins automatically update to the latest version at session start
- **improved:** Interactive shell shortcut now launches on Enter and shows an inline hint when "$" is armed
- **improved:** Split-view sidebar: the red close confirmation now reads `x again to close` (or `x again to exit CLI` on the last session) instead of `x close`, so a second press is clearly what closes
- **improved:** When the sandbox blocks a shell command and bypass is allowed, CLI offers to re-run it outside the sandbox without asking the model
- **improved:** /rewind no longer requires git and restores only the files Copilot changed, skipping any file whose contents no longer match what Copilot last wrote, with a conversation-only or conversation + files choice
- **improved:** New sandbox setting `allowDevToolCaches` (on by default): grants sandboxed builds access to toolchain caches, registries, and installs so builds work without extra setup. Set false to opt out.
- **improved:** Honor explicit GitHub MCP toolset/tool config: keep gh-overlap tools and stop steering to the gh CLI when you opt in
- **improved:** Warn on startup about unknown top-level keys in user settings.json (e.g. a misspelled setting) instead of silently ignoring them
- **improved:** Shell completion for --model now suggests auto and supported model names
- **improved:** Render long session transcripts progressively to keep scrolling responsive
- **improved:** Resuming a long session is dramatically faster and far lighter on memory, because its history is now read once at startup (in parallel, across CPU cores) instead of being re-read in full for every check the CLI runs before it can paint. In our benchmark a 230MB, 74k-event transcript came back in well under a second instead of about ten, at roughly a quarter of the peak memory; the exact gain depends on your machine's core count and disk
- **fixed:** Copilot login now defaults to the browser flow for local desktop subprocesses without a TTY, including IDE integrations, while remote and headless environments continue using device code
- **fixed:** Extension slash commands run their handler exactly once per invocation when several extensions are loaded
- **fixed:** A run whose prompt is piped over stdin now treats its `sessionEnd` hook the same way `-p` does: the hook fires once per completed agent turn with `reason` `complete` (or `error` if the turn failed), instead of once at shutdown with `user_exit`. As with `-p`, a piped run that exits before completing a turn fires no `sessionEnd` hook
- **fixed:** Disabling the sandbox from a bypass prompt applies only to that session; new sessions start sandboxed again
- **fixed:** Managed settings now fall back to the persistent cache whenever a server-managed settings fetch fails for any reason (network error, a non-success HTTP status, or a malformed/unparseable response), and fail open — starting without the unconfirmed server restriction rather than the prior fail-closed behavior — when no usable cached policy is available
- **fixed:** Ctrl+Q now enqueues the highlighted mid-text skill completion instead of the partial token
- **fixed:** Switching sessions no longer restarts MCP servers or rebuilds hook state, so a turn running in another session is never halted with a stale-hook error
- **fixed:** Refresh deferred MCP tools after OAuth authentication

### 1.0.77

Added: 2; improved: 2; fixed: 1.

- **added:** Add a browser-based (web) OAuth login flow, now the default for `copilot login` on local interactive terminals (device code remains the default on remote/headless terminals). Use `--web-flow`/`--device-code` to force a mode, or pick one in the interactive `/login` command
- **added:** Support enforcing managed sandbox policy via macOS and Windows native MDM settings
- **improved:** Ctrl+G opens your editor to edit ask_user freeform answers without closing the prompt
- **improved:** Allow reasoning effort to be omitted so the server can select the default
- **fixed:** Unconditional autopilot approval now disables sandbox for the current session when bypass is allowed

### 1.0.76

Added: 9; improved: 10; fixed: 14.

- **added:** Add enable/disable controls in /plugins for plugins, instructions, agents, LSP servers, and hooks
- **added:** Add support for the grok-4.5 model
- **added:** Add a directable queue manager (staff) to reorder, edit, remove, repeat, and immediately send queued messages
- **added:** New Sessions sidebar for managing multiple concurrent sessions: switch between them, spawn new ones, and see their status at a glance. Turn it on with experimental mode (`/experimental on`).
- **added:** Enterprise administrators can enforce a restrictive sandbox floor: managed settings tighten (but never loosen) the user's sandbox policy, and the `/sandbox` dialog surfaces the org-configured managed values with locked fields and managed filesystem paths so admins can confirm what is enforced.
- **added:** Voice mode pauses playing media before recording and resumes it afterward, where supported (macOS and Windows)
- **added:** Show the number of active scheduled prompts in the footer
- **added:** Add /limits predict to suggest a session AI-credit limit from similar sessions.
- **added:** Add configurable timed refreshes for custom status-line commands
- **improved:** When an update is auto-downloaded, the notification suggests /restart and drops the warning color
- **improved:** /diff scrolls and syntax-highlights large multi-file diffs faster
- **improved:** Split-view sidebar: hover-to-focus is now off by default (opt in with `sidebar.hoverFocus`), the active session card is accented by default (opt out with `sidebar.accentActiveSession`), and the closed-state `open sidebar` hint always renders in the neutral hint color
- **improved:** `web_fetch` now follows HTTP redirects instead of failing, asking permission for the redirect target when it is on a different origin and showing where the redirect came from
- **improved:** Sandboxed searches now offer an immediate bypass prompt and avoid duplicate bypass prompts.
- **improved:** web_fetch routes through the configured sandbox proxy when outbound is allowed, and denies egress when network.allowOutbound is false (a proxy no longer overrides the user's outbound policy); when a proxied fetch fails it warns that curl/wget share the same proxy, and suggests requestSandboxBypass only when the sandbox proxy itself is unreachable
- **improved:** Improve subagent delegation for small tasks and parallel work
- **improved:** Queue mid-turn /model changes and apply them after the current response finishes
- **improved:** MCP tools load faster from definition-scoped snapshots, with process-wide and per-server cache opt-outs.
- **improved:** Autopilot stays selected after task_complete by default; set stayInAutopilot to false to return to interactive mode after each task
- **fixed:** Sandbox denied paths are enforced for relative and symlinked entries on macOS and Linux (Windows cannot deny per path)
- **fixed:** Unsent prompt text now stays with the session it was typed for (for the rest of the CLI session) instead of following you to the session you switch to
- **fixed:** Resuming a session now restores its autopilot or plan mode instead of reverting to interactive, so the autopilot-only `task_complete` tool stays available and the mode matches the session you left
- **fixed:** URL permission prompts now keep their sandbox-bypass warning and the model's reason when a host integration rebuilds the prompt, so an elevated fetch is no longer shown as an ordinary one
- **fixed:** Sessions no longer fail every turn with "Holder terminated during creation" after a subagent finishes
- **fixed:** A `userPromptSubmitted` hook returning a non-string value for `modifiedPrompt`, `modifiedTransformedPrompt`, or a handled `responseContent` no longer corrupts the session; the value is ignored, a type-only warning naming the field is logged, an empty-string replacement is rejected instead of blanking the model-facing content, a hook that sets `handled` without a usable `responseContent` is now diagnosed instead of silently falling through to the model, and a `null` `additionalContext` is treated as absent instead of being injected as the literal text `null`; hook output is also bounded at 10 MiB per invocation, so an HTTP or command hook returning an unbounded response can no longer exhaust memory or leave an oversized session behind
- **fixed:** Show recent shell output for large commands that write to a file
- **fixed:** Changing the `mouse` setting mid-session now takes effect immediately, from both `/settings mouse on|off` and the `/settings` dialog, instead of being saved but ignored until the CLI restarted
- **fixed:** Restore the early warning when unreclaimable system and tool context nears the limit, before automatic compaction is blocked
- **fixed:** Session working directory no longer reverts to the original checkout shortly after `/worktree` switches into a new worktree

### 1.0.75

Added: 1; improved: 0; fixed: 0.

- **added:** Add support for Claude Opus 5

### 1.0.74

Added: 4; improved: 9; fixed: 7.

- **added:** Add support for Open Plugin Spec v1 plugin manifests and mcp.json configuration
- **added:** Show a first-run splash to opt into the default sandbox
- **added:** Adding support for gemini-3.6-flash
- **added:** Add `/model plan` (or `/model --plan`) to pick a model used while in plan mode; pass a model id, `off` to clear, or no id to open the picker. Reverts to the session model when you leave plan mode.
- **improved:** Subagent timelines identify whether prompts came from the main agent or another subagent
- **improved:** When multiplexing sessions, a session's open dialog no longer leaks into another session; eligible pickers reopen when you switch back
- **improved:** The `$` interactive shell shortcut now opens a shell even while the agent is working
- **improved:** Fully honor the skill disable-model-invocation flag
- **improved:** Warn when a participating language server reports a different symbol than the one requested
- **improved:** Steering interrupts shell output waits without stopping the running command
- **improved:** Increase the Responses request size limit
- **improved:** Plan mode now allows session-folder planning artifacts while still blocking clear file mutations outside the session folder.
- **improved:** Resume search matches session titles even when whitespace differs
- **fixed:** Typing `?` while the /search bar is open enters it as text instead of opening quick help
- **fixed:** IDE integration reconnects reliably when the CLI reloads MCP servers or changes directory
- **fixed:** Multi-turn subagent timelines show every prompt and response in the correct order after reopening /tasks
- **fixed:** The `/mcp add` and `/mcp edit` wizard now preserves `=` characters in environment variable values (such as base64 padding), so secrets and tokens are stored correctly.
- **fixed:** Remote session uploads stop retrying permanent Mission Control 400/404 responses
- **fixed:** Show Tab in /settings footer to switch scope tabs
- **fixed:** Downscale oversized tool-result images so CAPI Responses requests continue

### 1.0.73

Added: 0; improved: 1; fixed: 1.

- **improved:** Resolve relative links in custom agent instructions from the agent file location
- **fixed:** Anthropic subagents continue working when additional directories are configured

### 1.0.72

Added: 10; improved: 10; fixed: 24.

- **added:** Add opt-in git and gh authentication inside the OS sandbox
- **added:** Add `update`/`uninstall` verbs to `/plugins`, let `enable`/`disable`/`remove` target plugins, MCP servers, or skills via `--plugin`/`--mcp`/`--skill` flags or a positional kind, and support installing skills with `/plugins install --skill`
- **added:** Add a /plugins help command plus skill, MCP, and marketplace management for full /plugin parity
- **added:** Type $ at the prompt to open an interactive shell in the current session directory (enable it with `/settings shellShortcut on`; off by default)
- **added:** Install skills from the CLI with `copilot plugins install --skill <file, URL, or directory>` (add `--scope project` to a file or URL install to install into the repository)
- **added:** Show hex color codes written as inline code (e.g. `#FF0000`) as color swatches, and add a renderHexColors setting (on by default) to toggle hex-color swatches
- **added:** Add --plugin, --mcp, and --skill flags for plugin mutations
- **added:** Add skill removal support to `copilot plugins remove --skill`
- **added:** Multi-turn subagents are always enabled, so you can send follow-up messages to running agents
- **added:** Enable tool search for Claude Haiku 4.5+
- **improved:** Sandbox macOS keychain access now defaults off for tighter isolation; re-enable it in /sandbox if a command needs it
- **improved:** Show a connected message after a slow MCP server eventually connects
- **improved:** Show default values in /settings and let booleans cycle back to default
- **improved:** Require SSO for remote control when managed settings demand it
- **improved:** Add /model --session (-s) to change the model, reasoning effort, or context window for just the current session, leaving global settings unchanged.
- **improved:** The Sessions sidebar is now navigable with the keyboard and mouse (arrows open and focus it and move the selection, and Enter or a click switches to a session; press n to spawn a session or x twice to close one from the keyboard); /settings can disable it or stop restoring remembered sessions
- **improved:** Reveal full file paths when expanding compact editing rows
- **improved:** Make the plan-approval menu deterministic across models
- **improved:** Keep /add-dir directories visible in the agent context across turns
- **improved:** Deliver scheduled prompts as steering messages when the agent is busy
- **fixed:** An `agentStop` hook that always blocks no longer loops indefinitely: the CLI now ends the turn after 8 consecutive blocks, and `agentStop` hooks receive a `stop_hook_active` flag so they can detect a forced continuation and self-limit
- **fixed:** Lifecycle and subagent hook commands run in the current session directory after /cd
- **fixed:** Deleting an MCP server with /mcp delete stops its running background process
- **fixed:** Toggling /sandbox restarts only local MCP servers and leaves remote servers connected
- **fixed:** Command approvals no longer carry over to another repository after you switch with /cd
- **fixed:** Preserve pasted prompt content when changing models through Ctrl+X /model
- **fixed:** /worktree and /move no longer fail to create a worktree for an auto-generated branch name when many similarly-named branches or a leftover worktree directory already exist: the numeric-suffix search is no longer capped at 5 and now skips stale unregistered worktree directories (an explicitly supplied branch name still errors on a collision)
- **fixed:** /worktree <task> no longer intermittently runs the kickoff task in the main repo instead of the new worktree
- **fixed:** /worktree and /move now propagate folder trust to the new worktree before switching when the source is already trusted (avoiding a spurious folder-trust prompt in that case), /move addresses its git stash precisely so a concurrent stash can't misplace changes, and worktree creation skips a leftover directory instead of failing
- **fixed:** Show the exit resume hint for a renamed session with no messages when its workspace is only available as a cached snapshot
- **fixed:** Session exports keep angle brackets intact in inline code and top-level fenced code blocks
- **fixed:** Show MCP server status correctly for names like constructor and __proto__
- **fixed:** Keep the session highlight on the nearest live row when closing a session, in both the Sessions split view and the standalone Sessions tab
- **fixed:** `copilot skill list` now strips terminal control characters from skill names and descriptions, so a crafted skill can no longer inject ANSI escape sequences into the listing output.
- **fixed:** Mask secret values in /settings show output
- **fixed:** Modified vim keys (Ctrl+K, uppercase J/K) no longer move the selection in tool-permission prompts and other text-input select menus; only unmodified j/k, the arrow keys, and Ctrl+P/Ctrl+N navigate.

## Extracted package file changes

Changed paths: 69.

- `A	copilot-cli-pkg/animations/app-install-nudge.json.gz`
- `A	copilot-cli-pkg/animations/banner.json.gz`
- `A	copilot-cli-pkg/builtin-skills/github-pr-media/SKILL.md`
- `A	copilot-cli-pkg/builtin/github-pr-media/SKILL.md`
- `A	copilot-cli-pkg/copilot-sdk/copilotRequestHandler.d.ts`
- `A	copilot-cli-pkg/copilot-sdk/docs/factories.md`
- `A	copilot-cli-pkg/copilot-sdk/docs/factory-patterns.md`
- `A	copilot-cli-pkg/copilot-sdk/factory.d.ts`
- `A	copilot-cli-pkg/copilot-sdk/ffiRuntimeHost.d.ts`
- `A	copilot-cli-pkg/definitions/sidekick/test-sidekick-context-changed.yaml`
- `A	copilot-cli-pkg/prebuilds/linux-x64/copilot-runtime`
- `D	copilot-cli-pkg/clipboard/index.js`
- `D	copilot-cli-pkg/clipboard/node_modules/@teddyzhu/clipboard-linux-x64-gnu/README.md`
- `D	copilot-cli-pkg/clipboard/node_modules/@teddyzhu/clipboard-linux-x64-gnu/clipboard.linux-x64-gnu.node`
- `D	copilot-cli-pkg/clipboard/node_modules/@teddyzhu/clipboard-linux-x64-gnu/package.json`
- `D	copilot-cli-pkg/clipboard/node_modules/@teddyzhu/clipboard/LICENSE`
- `D	copilot-cli-pkg/clipboard/node_modules/@teddyzhu/clipboard/README.md`
- `D	copilot-cli-pkg/clipboard/node_modules/@teddyzhu/clipboard/clipboard.darwin-arm64.node`
- `D	copilot-cli-pkg/clipboard/node_modules/@teddyzhu/clipboard/clipboard.darwin-x64.node`
- `D	copilot-cli-pkg/clipboard/node_modules/@teddyzhu/clipboard/clipboard.linux-arm64-gnu.node`
- `D	copilot-cli-pkg/clipboard/node_modules/@teddyzhu/clipboard/clipboard.linux-x64-gnu.node`
- `D	copilot-cli-pkg/clipboard/node_modules/@teddyzhu/clipboard/clipboard.win32-arm64-msvc.node`
- `D	copilot-cli-pkg/clipboard/node_modules/@teddyzhu/clipboard/clipboard.win32-x64-msvc.node`
- `D	copilot-cli-pkg/clipboard/node_modules/@teddyzhu/clipboard/index.d.ts`
- `D	copilot-cli-pkg/clipboard/node_modules/@teddyzhu/clipboard/index.js`
- `D	copilot-cli-pkg/clipboard/node_modules/@teddyzhu/clipboard/package.json`
- `D	copilot-cli-pkg/napi-oop-runtime/index.js`
- `D	copilot-cli-pkg/napi-oop-runtime/node_modules/napi-oop-runtime/dist/codegen-cli.js`
- `D	copilot-cli-pkg/napi-oop-runtime/node_modules/napi-oop-runtime/dist/index.js`
- `D	copilot-cli-pkg/napi-oop-runtime/node_modules/napi-oop-runtime/dist/sync-worker.js`
- `D	copilot-cli-pkg/napi-oop-runtime/node_modules/napi-oop-runtime/package.json`
- `D	copilot-cli-pkg/sdk/index.d.ts`
- `M	copilot-cli-pkg/app.js`
- `M	copilot-cli-pkg/changelog.json`
- `M	copilot-cli-pkg/copilot-sdk/client.d.ts`
- `M	copilot-cli-pkg/copilot-sdk/docs/extensions.md`
- `M	copilot-cli-pkg/copilot-sdk/extension.d.ts`
- `M	copilot-cli-pkg/copilot-sdk/extension.js`
- `M	copilot-cli-pkg/copilot-sdk/generated/rpc.d.ts`
- `M	copilot-cli-pkg/copilot-sdk/generated/session-events.d.ts`
- `M	copilot-cli-pkg/copilot-sdk/index.d.ts`
- `M	copilot-cli-pkg/copilot-sdk/index.js`
- `M	copilot-cli-pkg/copilot-sdk/session.d.ts`
- `M	copilot-cli-pkg/copilot-sdk/sessionFsProvider.d.ts`
- `M	copilot-cli-pkg/copilot-sdk/types.d.ts`
- `M	copilot-cli-pkg/definitions/code-review.agent.yaml`
- `M	copilot-cli-pkg/definitions/explore.agent.yaml`
- `M	copilot-cli-pkg/definitions/research.agent.yaml`
- `M	copilot-cli-pkg/definitions/security-review.agent.yaml`
- `M	copilot-cli-pkg/definitions/sidekick/cloud-session-search.yaml`
- `M	copilot-cli-pkg/definitions/sidekick/github-context-memory.yaml`
- `M	copilot-cli-pkg/definitions/sidekick/session-search.yaml`
- `M	copilot-cli-pkg/definitions/sidekick/subconscious-agent.yaml`
- `M	copilot-cli-pkg/definitions/sidekick/test-sidekick-persistent.yaml`
- `M	copilot-cli-pkg/definitions/sidekick/test-sidekick-restart.yaml`
- `M	copilot-cli-pkg/definitions/sidekick/test-sidekick-trigger-once.yaml`
- `M	copilot-cli-pkg/index.js`
- `M	copilot-cli-pkg/package.json`
- `M	copilot-cli-pkg/prebuilds/linux-x64/cli-native.node`
- `M	copilot-cli-pkg/prebuilds/linux-x64/runtime.node`
- `M	copilot-cli-pkg/preloads/extension_bootstrap.mjs`
- `M	copilot-cli-pkg/schemas/api.schema.json`
- `M	copilot-cli-pkg/schemas/session-events.schema.json`
- `M	copilot-cli-pkg/sdk/index.js`
- `M	copilot-cli-pkg/sea-loader.js`
- `M	copilot-cli-pkg/tgrep/bin/linux-x64/tgrep`
- `M	copilot-cli-pkg/voice-engine.worker.js`
- `M	copilot-cli-pkg/voice-installer.worker.js`
- `M	copilot-cli-pkg/voice-server.js`

## New subsystem and module review

Detected candidates: 7.

Review and resolve every candidate in [Latest subsystem and module review](latest-subsystem-review.md). A new independent lifecycle, entrypoint, state model, protocol, or trust boundary should receive a focused page rather than being folded into unrelated update prose.

## Documentation checklist

- [ ] Confirm every documented behavior in current `copilot-cli-pkg/app.js`, SDK declarations, schemas, help, or packaged definitions.
- [ ] Update an existing owning page instead of duplicating a subsystem page.
- [ ] Refresh version-specific architecture claims and mark removed paths as historical when useful.
- [ ] Update `docs/SUMMARY.md`, section indexes, and `website/astro.config.mjs` when navigation changes.
- [ ] Update [documentation opportunities](../../docs/99-research-atlas/documentation-opportunities.md) with closed or residual gaps.
- [ ] Run `node scripts/check-docs.mjs` and `npm run build` from `website/`.

## Suggested triage order

1. User-visible slash commands and root flags.
2. Settings, feature gates, permissions, and sandbox policy.
3. SDK/RPC methods and durable session events.
4. MCP, plugins, extensions, tools, and built-in agents.
5. Model/provider and context-window behavior.
6. Native or out-of-process architecture migrations.
